import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
    marginHorizontal: 16,
    marginBottom: 16,
  },
  image: {
    width: '100%',
    height: 180,
    borderRadius: 10,
  },
});

